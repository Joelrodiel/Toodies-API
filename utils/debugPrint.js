var moment = require('moment');

var colors = {
	reset: "\x1b[0m",
	red: "\x1b[31m",
	green: "\x1b[32m"
}

module.exports.print(id) {
	const timeHeader = "[ " + moment().format() + " ]

	switch(id) {
		case 1:
			console.log(colors.green, timeheader,reset,
			break;
		case 2:
			break;
		case 3:
			break;
		case 4:
			break;
		default:
			break;
}
